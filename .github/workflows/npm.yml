name: Build React

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install Dependencies
        run: npm install

      - name: Build React Application
        run: npm run build

      - name: Azure Login
        run: az login --service-principal -u '53f0cb1b-8b96-42db-94a3-d3d427b51668' -p 'akb8Q~z9rLBGqrtTbwbLAh1HY5VD45D5pW_d~cbW' --tenant 'c02f747c-778b-4b2f-bb51-5fc839441a1e' && az artifacts universal publish --organization https://dev.azure.com/pradeepcp000 --project "Git-hub_action" --feed git_action --name react-build --version 1.0.0 --description "React build artifacts" --path build/ --scope project

      # - name: Upload Artifacts to Azure
      #   run: |
      #    az artifacts universal publish --organization https://dev.azure.com/pradeepcp000 --project "Git-hub_action" --feed git_action --name react-build --version 1.0.0 --description "React build artifacts" --path build/ --scope project
