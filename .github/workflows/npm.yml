name: Build and Push to Artifactory

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install Dependencies
        run: npm install

      - name: Build React App
        run: npm run build

      - name: Create Tar Archive
        run: tar -czvf build.tar.gz build/

      # - name: List Tar Archive Contents
      #   run: tar -tzf build.tar

      - name: Push to Artifactory
        run: |
          curl -u harirajan8940@gmail.com:Admin@1234  -T build/* "https://gitaction12cp.jfrog.io/artifactory/git_action-npm-local/"
         # curl -u harirajan8940@gmail.com:Admin@1234 -X PUT "https://gitaction12cp.jfrog.io/artifactory/git_action-npm-local/build.tar.gz" -T build.tar.gz
        #  curl -u harirajan8940@gmail.com:cmVmdGtuOjAxOjE3NDcxMTkyMTU6Q0Z2ZTJzZHNOakM0NDNBbTExWHBzcHRNS2hZ -T build 'https://gitaction12cp.jfrog.io/artifactory/git_action-npm-local/build.tar.gz'


