name: Build and Push to Artifactory

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Build React app
      run: npm run build

    - name: Check JFrog Login
      run: |
       curl -u harirajan8940@gmail.com:cmVmdGtuOjAxOjE3NDcxMTkyMTU6Q0Z2ZTJzZHNOakM0NDNBbTExWHBzcHRNS2hZ -T build 'https://gitaction12cp.jfrog.io/artifactory/cmVmdGtuOjAxOjE3NDcxMTkyMTU6Q0Z2ZTJzZHNOakM0NDNBbTExWHBzcHRNS2hZ/build'
