name: Build and Deploy to EC2

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Previous steps...

      - name: Download Artifact from Artifactory
        run: |
          curl -u harirajan8940@gmail.com:cmVmdGtuOjAxOjE3NDY4NzQ3NzA6Tm11eERHV1lGdUJKMVFQRnJHclZOZ1c1SU5L -o build.tar.gz "https://gitaction12cp.jfrog.io/artifactory/git_action-npm-local/build.tar.gz"

      - name: Transfer Artifact to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USR }}
          password: ${{ secrets.VM_PWD }}
          port: ${{ secrets.VM_PORT }} # Optional, default is 22
          script: |
            scp build.tar.gz ${{ secrets.AWS_VM_USERNAME }}@${{ secrets.AWS_VM_HOST }}:/var/www/html
